import { PlayerStats, PlayerData } from '../../types';

export interface Database {
    createPlayerStats: () => void;
    // retrievePlayerStats: () => PlayerStats;

    // CRUD => Create, Retrieve, Update, Delete
}